<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Staff</title>
    <style>
        table {
            border-collapse: collapse;
        }

        th {
            border: solid 1px;
            padding: 0.5em;
        }

        td {
            border: solid 1px;
            padding: 0.5em;
        }
    </style>
</head>

<body>
    <h1>Staff</h1>

    <table>
        <tr>
            <th>id</th>
            <th>name</th>
            <th>address</th>
        </tr>
        <% data.forEach((item) => { %>
        <tr>
            <td><%= item.id %></td>
            <td><%= item.name %></td>
            <td><%= item.address %></td>
        </tr>
        <% });%>
    </table>

</body>

</html>